apiVersion: v1
kind: Service
metadata:
  name: go-demo
  namespace: go-demo
  labels:
    app: go-demo
spec:
  type: ClusterIP
  selector:
    app: go-demo
  ports:
    - name: http
      port: 80
      targetPort: http
